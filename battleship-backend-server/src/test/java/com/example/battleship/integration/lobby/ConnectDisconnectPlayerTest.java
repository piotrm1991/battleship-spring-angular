package com.example.battleship.integration.lobby;

public class ConnectDisconnectPlayerTest {
  // TODO: connect disconnect player tests.
  /*
   * - player can only join one game room.
   * - player can't join game room as both, player one and player two
   * - websockets, that aren't used anymore should be deleted
   * -
   */
}
